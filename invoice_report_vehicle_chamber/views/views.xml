<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="invoice_report_vehicle_chamber.list">
      <field name="name">invoice_report_vehicle_chamber list</field>
      <field name="model">invoice_report_vehicle_chamber.invoice_report_vehicle_chamber</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="invoice_report_vehicle_chamber.action_window">
      <field name="name">invoice_report_vehicle_chamber window</field>
      <field name="res_model">invoice_report_vehicle_chamber.invoice_report_vehicle_chamber</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="invoice_report_vehicle_chamber.action_server">
      <field name="name">invoice_report_vehicle_chamber server</field>
      <field name="model_id" ref="model_invoice_report_vehicle_chamber_invoice_report_vehicle_chamber"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="invoice_report_vehicle_chamber" id="invoice_report_vehicle_chamber.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="invoice_report_vehicle_chamber.menu_1" parent="invoice_report_vehicle_chamber.menu_root"/>
    <menuitem name="Menu 2" id="invoice_report_vehicle_chamber.menu_2" parent="invoice_report_vehicle_chamber.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="invoice_report_vehicle_chamber.menu_1_list" parent="invoice_report_vehicle_chamber.menu_1"
              action="invoice_report_vehicle_chamber.action_window"/>
    <menuitem name="Server to list" id="invoice_report_vehicle_chamber" parent="invoice_report_vehicle_chamber.menu_2"
              action="invoice_report_vehicle_chamber.action_server"/>
    -->
  </data>
</odoo>