<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="station.list">
      <field name="name">station list</field>
      <field name="model">station.station</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="station.action_window">
      <field name="name">station window</field>
      <field name="res_model">station.station</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="station.action_server">
      <field name="name">station server</field>
      <field name="model_id" ref="model_station_station"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="station" id="station.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="station.menu_1" parent="station.menu_root"/>
    <menuitem name="Menu 2" id="station.menu_2" parent="station.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="station.menu_1_list" parent="station.menu_1"
              action="station.action_window"/>
    <menuitem name="Server to list" id="station" parent="station.menu_2"
              action="station.action_server"/>
    -->
  </data>
</odoo>