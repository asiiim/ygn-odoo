<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="custom_filters_stock.list">
      <field name="name">custom_filters_stock list</field>
      <field name="model">custom_filters_stock.custom_filters_stock</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="custom_filters_stock.action_window">
      <field name="name">custom_filters_stock window</field>
      <field name="res_model">custom_filters_stock.custom_filters_stock</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="custom_filters_stock.action_server">
      <field name="name">custom_filters_stock server</field>
      <field name="model_id" ref="model_custom_filters_stock_custom_filters_stock"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="custom_filters_stock" id="custom_filters_stock.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="custom_filters_stock.menu_1" parent="custom_filters_stock.menu_root"/>
    <menuitem name="Menu 2" id="custom_filters_stock.menu_2" parent="custom_filters_stock.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="custom_filters_stock.menu_1_list" parent="custom_filters_stock.menu_1"
              action="custom_filters_stock.action_window"/>
    <menuitem name="Server to list" id="custom_filters_stock" parent="custom_filters_stock.menu_2"
              action="custom_filters_stock.action_server"/>
    -->
  </data>
</odoo>