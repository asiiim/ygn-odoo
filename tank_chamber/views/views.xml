<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="tank_chamber.list">
      <field name="name">tank_chamber list</field>
      <field name="model">tank_chamber.tank_chamber</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="tank_chamber.action_window">
      <field name="name">tank_chamber window</field>
      <field name="res_model">tank_chamber.tank_chamber</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="tank_chamber.action_server">
      <field name="name">tank_chamber server</field>
      <field name="model_id" ref="model_tank_chamber_tank_chamber"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="tank_chamber" id="tank_chamber.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="tank_chamber.menu_1" parent="tank_chamber.menu_root"/>
    <menuitem name="Menu 2" id="tank_chamber.menu_2" parent="tank_chamber.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="tank_chamber.menu_1_list" parent="tank_chamber.menu_1"
              action="tank_chamber.action_window"/>
    <menuitem name="Server to list" id="tank_chamber" parent="tank_chamber.menu_2"
              action="tank_chamber.action_server"/>
    -->
  </data>
</odoo>