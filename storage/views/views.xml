<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="storage.list">
      <field name="name">storage list</field>
      <field name="model">storage.storage</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="storage.action_window">
      <field name="name">storage window</field>
      <field name="res_model">storage.storage</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="storage.action_server">
      <field name="name">storage server</field>
      <field name="model_id" ref="model_storage_storage"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="storage" id="storage.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="storage.menu_1" parent="storage.menu_root"/>
    <menuitem name="Menu 2" id="storage.menu_2" parent="storage.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="storage.menu_1_list" parent="storage.menu_1"
              action="storage.action_window"/>
    <menuitem name="Server to list" id="storage" parent="storage.menu_2"
              action="storage.action_server"/>
    -->
  </data>
</odoo>