<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="product_template_sales_price_form_view" model="ir.ui.view">
        <field name="name">product.template.sales.price.form</field>
        <field name="model">product.template</field>
        <field name="mode">primary</field>
        <field name="priority" eval="7" />
        <field name="inherit_id" ref="product.product_template_only_form_view" />
        <field name="arch" type="xml">
            <xpath expr="//div[@name='options']" postion="after">
                <div>
                    <field name="is_base"/>
                    <label for="is_base"/>
                </div>
                <div attrs="{'invisible': [('is_base', '=', True)]}">
                    <field name="can_depend_base"/>
                    <label for="can_depend_base"/>
                </div>
            </xpath>
        </field>
    </record>

    <record id="product.product_normal_form_view" model="ir.ui.view">
        <field name="name">product.product.form</field>
        <field name="model">product.product</field>
        <field name="mode">primary</field>
        <field eval="7" name="priority"/>
        <field name="inherit_id" ref="product.product_template_form_view"/>
        <field name="arch" type="xml">
            <form position="attributes">
                <attribute name="string">Product Variant</attribute>
            </form>
            <field name="type" position="after">
                <field name="default_code"/>
                <field name="barcode"/>
            </field>
            <field name="list_price" position="attributes">
                <attribute name="name">lst_price</attribute>
                <attribute name="attrs">{'readonly': [('product_variant_count', '&gt;', 1)]}</attribute>
            </field>
            <group name="packaging" position="attributes">
                <attribute name="attrs">{'invisible': 0}</attribute>
            </group>
            <field name="name" position="after">
                <field name="product_tmpl_id" class="oe_inline" readonly="1" invisible="1" attrs="{'required': [('id', '!=', False)]}"/>
            </field>
            <field name="item_ids" position="inside">
                <field name="pricelist_item_ids" widget="many2many">
                    <tree string="Pricelist Items">
                        <field name="pricelist_id"/>
                        <field name="name" string="Applicable On"/>
                        <field name="min_quantity"/>
                        <field name="date_start"/>
                        <field name="date_end"/>
                        <field name="price" string="Price"/>
                        <field name="base" invisible="1"/>
                        <field name="sequence" invisible="1"/>
                        <field name="price_discount" invisible="1"/>
                        <field name="applied_on" invisible="1"/>
                        <field name="compute_price" invisible="1"/>
                    </tree>
                </field>
            </field>
            <xpath expr="//div[hasclass('oe_title')]" position="inside">
                <field name="attribute_value_ids" widget="many2many_tags" readonly="1"
                    groups="product.group_product_variant"/>
            </xpath>

            <!-- Customize Start -->
            <xpath expr="//page[@name='general_information']" position="inside">
                <field name="is_base" invisible="1"/>
                <field name="can_depend_base" invisible="1"/>
                <group>
                    <group string="Sales Price Logistics" attrs="{'invisible': [('is_base', '=', True)]}">
                        <field name="base_product_id" attrs="{'invisible': ['|', ('can_depend_base', '=', False), ('is_base', '=', True)]}" />
                        <field name="weight" string="Readymade Weight" />
                        <field name="loss" />
                        <field name="wage" widget="monetary"/>
                        <field name="gems_cost" widget="monetary" />
                    </group>
                </group>
            </xpath>
            <!-- End -->

        </field>
    </record>

    <record id="product.product_variant_easy_edit_view" model="ir.ui.view">
        <field name="name">product.product.view.form.easy</field>
        <field name="model">product.product</field>
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <form string="Variant Information">
                <sheet>
                    <field name="id" invisible="1"/>
                    <field name="image_medium" widget="image" class="oe_avatar"/>
                    <div class="oe_title">
                        <label class="oe_edit_only" for="name" string="Product Name"/>
                        <h1><field name="name" placeholder="e.g. Odoo Enterprise Subscription"/></h1>
                    </div>
                    <group>
                        <p>All general settings about this product are managed on <button name="open_product_template" type="object" string="the product template" class="oe_link"/></p>
                    </group>
                    <group string="Attributes">
                        <field name="attribute_value_ids" widget="many2many_tags" readonly="1"/>
                    </group>
                    <group>
                        <group name="codes" string="Codes">
                            <field name="barcode"/>
                            <field name="default_code"/>
                        </group>
                        <group>
                            <field name="active"/>
                            <field name="type" invisible="1"/>
                        </group>
                    </group>
                    <group>
                        <group name="pricing" string="Pricing">
                            <field name="product_variant_count" invisible="1"/>
                            <field name="lst_price" widget='monetary' options="{'currency_field': 'currency_id', 'field_digits': True}" attrs="{'readonly': [('product_variant_count', '&gt;', 1)]}"/>
                            <field name="standard_price" widget='monetary' options="{'currency_field': 'cost_currency_id', 'field_digits': True}"/>
                            <field name="currency_id" invisible='1'/>
                            <field name="cost_currency_id" invisible="1"/>
                        </group>

                        <!-- Customized Part -->
                        <group name="sales_price_variation">
                            <field name="is_base" invisible="1"/>
                            <field name="can_depend_base" invisible="1"/>
                            <field name="fix_price" invisible="1"/>
                            <field name="weight" string="Readymade Weight" attrs="{'invisible': [('is_base', '=', True)]}"/>
                            <field name="loss" attrs="{'invisible': [('is_base', '=', True)]}"/>
                            <field name="wage" widget="monetary" attrs="{'invisible': [('is_base', '=', True)]}"/>
                            <field name="gems_cost" widget="monetary" attrs="{'invisible': [('is_base', '=', True)]}"/>
                            <field name="base_product_id" attrs="{'invisible': ['|', ('can_depend_base', '=', False), ('is_base', '=', True)]}" />
                        </group>
                        <!-- Customized part end -->

                    </group>
                    <group name="packaging" string="Packaging"
                        colspan="4"
                        groups="product.group_stock_packaging">
                        <field name="packaging_ids" nolabel="1" context="{'default_product_id': id, 'tree_view_ref':'product.product_packaging_tree_view', 'form_view_ref':'product.product_packaging_form_view'}"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>